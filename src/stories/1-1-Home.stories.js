import React from 'react';
import { action } from '@storybook/addon-actions';
import Navigation from 'components/Navigation';

export default {
	title: 'Home',
	// component: Home,
};

export const Navigation_ = () => <Navigation />;
